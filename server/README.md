# Server

Node.js backend service root.

The backend project has not been created yet. Once it exists, record the Node.js version, startup command, test command, environment variables, and production deployment notes here or in `docs/deployment.md`.
