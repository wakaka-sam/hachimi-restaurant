# Hachimi Restaurant

Repository for 小动物餐厅.

Production client targets share one Cocos codebase under `client/cocos/`: Web, WeChat Mini Game, and Douyin Mini Game are all Cocos build outputs. The `client/web/` directory is only a temporary backend/gameplay debug harness until Cocos Web builds are available in this environment.
